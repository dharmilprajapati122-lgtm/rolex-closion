# Aurelion Perpetual

Static luxury watch landing page with a scroll-scrubbed hero animation built from the provided 241 PNG frames.

Open `index.html` directly in a browser, or serve this folder with any static file server for the smoothest local preview.
